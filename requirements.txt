# CPU-only torch — keeps Docker image ~800 MB instead of 5 GB
# Must be installed from the PyTorch index, not PyPI
torch==2.2.2+cpu
torchvision==0.17.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
Pillow==10.3.0
numpy==1.26.4